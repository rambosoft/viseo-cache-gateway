# Copy to .env for local-only values. Never commit secrets.
NODE_ENV=development

# Added when the database foundation is configured.
DATABASE_URL=

# Added when Cloudflare resources are configured.
CLOUDFLARE_ACCOUNT_ID=
